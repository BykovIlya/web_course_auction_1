extends layout
block title
    | Index page
block content
    include ./header
    div(class='content')
        h3(align="center") Аукцион по картине #{auction["picName"]} cocтоится #{auction["dateBegin"]} в #{auction["timeBegin"]}
        br
        h3(align="center") Участники аукциона:
        br
        for par in participants
            for pic in par["auction"]
                if (pic["picture"] == auction["picName"])
                    h4(align="center") #{par["name"]}

    include ./footer